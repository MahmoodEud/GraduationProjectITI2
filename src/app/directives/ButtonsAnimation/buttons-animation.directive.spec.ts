import { ButtonsAnimationDirective } from './buttons-animation.directive';

describe('ButtonsAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
